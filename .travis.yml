language: node_js
node_js:
  - 10
# cache:
#   directories:
#     - node_modules
# install:
#   - npm install -g codecov
script:
  - npm i
  - npm run deploy  # deploy automatically runs pre-deploy, which is actually npm run build
  - npm run test-codecov